# working conventions for the owner and the assistant, not for readers of the repository
CLAUDE.md

# the built binary, anchored so it cannot swallow cmd/dagreach or internal/dagreach
/dagreach
/dagreach.exe
/dist/
*.test
